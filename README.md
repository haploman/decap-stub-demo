# Decap Stub Demo (Cloudflare Pages)

This is a minimal static site that uses Decap CMS to edit `content/home.json`.

## What you edit
- Visit `/admin`
- Login with GitHub
- Edit the Homepage fields
- Publish

## Important
Update `admin/config.yml` with your GitHub `repo:` value.
